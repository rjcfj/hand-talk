{
  "name": "hand-talk",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^14.0.0",
    "pnpm": "^8.0.0"
  },
  "scripts": {
    "test": "pnpm -r --parallel test",
    "test:ci": "pnpm -r --serial test -- --runInBand",
    "lint-staged:install": "husky install"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
